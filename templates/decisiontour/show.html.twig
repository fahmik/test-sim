{% extends 'base.html.twig' %}

{% block title %}Decisiontour{% endblock %}

{% block body %}
    <h1>Decisiontour</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ decisiontour.id }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ decisiontour.valeur }}</td>
            </tr>
            <tr>
                <th>Valhist</th>
                <td>{{ decisiontour.valhist }}</td>
            </tr>
            <tr>
                <th>Decision</th>
                <td>{{ decisiontour.decision }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('decisiontour_index') }}">back to list</a>

    <a href="{{ path('decisiontour_edit', {'id': decisiontour.id}) }}">edit</a>

    {{ include('decisiontour/_delete_form.html.twig') }}
{% endblock %}
